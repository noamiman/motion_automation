Analyze ONLY the data in <input>…</input>. In 1–2 short sentences, state the person’s most likely immediate intention based strictly on posture/movement; do not speculate. Always include the action time as HH:MM (24-hour): extract from the data; if multiple times exist, use the earliest tied to the action; if none, write ‘intention unclear at 00:00’. End your first sentence with “at HH:MM”. Output only the sentences.

<input>
{sentence}
</input>
